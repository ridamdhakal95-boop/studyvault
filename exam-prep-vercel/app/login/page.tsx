"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { BookOpen, LockKeyhole } from "lucide-react";
export default function LoginPage(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setError(d.error||"Login failed");setBusy(false);return}router.push("/admin");router.refresh()}return <main className="login-page"><div className="login-card"><Link className="brand" href="/"><span className="logo"><BookOpen size={19}/></span>ExamPrep</Link><h1 style={{fontSize:34,marginTop:26}}>Admin sign in</h1><p className="muted">Manage chapters and publish study material securely.</p><form onSubmit={submit} style={{display:"grid",gap:15,marginTop:24}}><div className="field"><label>Email</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></div><div className="field"><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></div>{error&&<div className="badge red">{error}</div>}<button className="btn btn-primary" disabled={busy}><LockKeyhole size={16}/>{busy?"Signing in…":"Sign in"}</button></form></div></main>}
